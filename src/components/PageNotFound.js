
import Hero from "./hero";

const PageNotFound = () => {
    return (
        <div>
            <Hero text = "Sorry the page you are looking for does not exist"/>
            
        </div>
    )
}

export default PageNotFound;